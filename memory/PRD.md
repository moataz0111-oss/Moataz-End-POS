# نظام إدارة المطاعم - Maestro EGP

## الوصف
نظام متكامل لإدارة المطاعم يشمل نقاط البيع، إدارة المخزون، المشتريات، التصنيع، طلبات الفروع، التوصيل، والتقارير.

---

## آخر التحديثات (8 فبراير 2026) - الجزء 28

### ✅ إصلاح نهائي لتطبيق الزبائن PWA

#### 1. إنشاء صفحة تثبيت منفصلة للزبائن
- **المسار الجديد**: `/customer-app/`
- **الملفات**:
  - `/app/frontend/public/customer-app/index.html` - صفحة تثبيت جميلة بتصميم برتقالي
  - `/app/frontend/public/customer-app/manifest.json` - manifest منفصل تماماً
- **السلوك**: عند تثبيت التطبيق من `/customer-app/`، يفتح على قائمة الطعام `/menu` وليس صفحة تسجيل الدخول
- **ملاحظة هامة**: يجب على المستخدمين حذف التطبيق القديم ثم تثبيته من الرابط الجديد

#### 2. إضافة رابط تثبيت التطبيق في لوحة التحكم
- نافذة "قائمة العملاء" تعرض الآن رابطين:
  - **رابط تثبيت التطبيق ⭐** - `/customer-app/` (بخلفية خضراء)
  - رابط القائمة العادي - `/menu/{slug}`

#### 3. تحسين تحديد الموقع في صفحة الـ Checkout
- **زر جديد**: "📍 موقعي الحالي" (أخضر) - يحدد الموقع مباشرة
- **زر موجود**: "اختر من الخريطة" - لاختيار الموقع يدوياً
- **عرض حالة الموقع**: رسالة تأكيد خضراء عند نجاح تحديد الموقع
- **رسائل خطأ واضحة**: في حال رفض الإذن أو فشل التحديد

#### 4. ميزة سجل طلبات الزبون (مكتملة)
- **API**: `GET /api/customer/orders/history?tenant_id={slug}&phone={phone}`
- **عزل البيانات**: كل زبون يرى فقط طلباته الخاصة بناءً على رقم هاتفه
- **الواجهة**: زر History يظهر في header مع عدد الطلبات السابقة
- **ميزة إعادة الطلب**: يمكن للزبون إعادة طلب سابق بنقرة واحدة

#### نتائج الاختبار - Iteration 51:
- **Backend**: 100%
- **Frontend**: 100%
- جميع الاختبارات نجحت بدون مشاكل

#### خطوات تثبيت التطبيق للزبائن:
1. حذف التطبيق القديم (إن وُجد)
2. فتح الرابط: `https://restupos.preview.emergentagent.com/customer-app/`
3. في Safari: النقر على زر المشاركة → "إضافة للشاشة الرئيسية"
4. التطبيق سيفتح على قائمة الطعام

---

## آخر التحديثات (8 فبراير 2026) - الجزء 27

### ✅ إصلاح تطبيق الزبائن (PWA)

#### 1. إنشاء صفحة اختيار المطعم `/menu`
- **الملف الجديد**: `/app/frontend/src/pages/RestaurantSelector.js`
- **الوظيفة**: عرض قائمة المطاعم المتاحة للزبائن
- **PWA**: يفتح على `/menu` بدلاً من صفحة تسجيل الدخول

#### 2. إضافة API جديد
- **Endpoint**: `GET /api/customer/restaurants`
- **الوظيفة**: جلب قائمة المطاعم المتاحة مع معلوماتها

#### 3. تدفق طلب العميل الكامل (تم اختباره بنجاح)
1. `/menu` ← عرض قائمة المطاعم
2. اختيار المطعم ← `/menu/{slug}` عرض الفروع
3. اختيار الفرع ← عرض قائمة الطعام
4. إضافة منتجات للسلة
5. إتمام الطلب ← عرض صفحة التتبع
6. الطلب يظهر في:
   - صفحة الطلبات `/orders`
   - شاشة المطبخ `/kitchen`

#### 4. إصلاحات Backend (من وكيل الاختبار)
- إضافة قيم افتراضية لـ `OrderResponse` model
- إصلاح تحويل `menu_slug` إلى `tenant_id` الصحيح

---

## آخر التحديثات (8 فبراير 2026) - الجزء 26

### ✅ إصلاحات جوهرية للتقارير والطاولات

#### 1. إصلاح أسماء المنتجات في التقارير
- **المشكلة:** التقارير كانت تعرض "Unknown" بدلاً من أسماء المنتجات
- **السبب:** ملف `routes/reports_routes.py` كان يستخدم `item.get("product_name")` بدلاً من `item.get("name")`
- **الإصلاح:** تعديل السطر 100 ليصبح `item.get("name", "غير معروف")`
- **النتيجة:** التقارير تعرض الآن "قهوة عربية" و "شاي" بشكل صحيح

#### 2. إعادة هيكلة الطاولات
- **الإجراء:** حذف جميع الطاولات القديمة وإنشاء طاولات جديدة لكل فرع
- **النتيجة:** 
  - **Aljadreia**: 5 طاولات (3 داخلي، 2 خارجي)
  - **Alsaydaia**: 5 طاولات (3 داخلي، 2 خارجي)
  - **إجمالي**: 21 طاولة (مع الطاولات المنشأة أثناء الاختبار)

#### 3. إزالة الـ endpoints المكررة
- **الإجراء:** حذف الـ endpoints المضافة خطأً في `server.py` (9550-10102)
- **السبب:** كانت تتعارض مع `routes/reports_routes.py`

#### نتائج الاختبار - Iteration 49:
- **Backend**: 100%
- **Frontend**: 100%
- **جميع المشاكل المُبلغ عنها تم إصلاحها**

---

## آخر التحديثات (8 فبراير 2026) - الجزء 25

### ✅ إصلاحات شاملة لقاعدة البيانات والنظام

#### 1. إصلاح tenant للمستخدم التجريبي
- **المشكلة:** `/menu/demo-maestro` لا يجد الـ tenant
- **الإصلاح:** إضافة `menu_slug: "demo-maestro"` للـ tenant
- **النتيجة:** تطبيق الزبائن يعمل الآن بشكل صحيح

#### 2. نقل الطاولات للفروع الصحيحة
- **المشكلة:** الطاولات مرتبطة بـ "الفرع الرئيسي" المخفي
- **الإصلاح:** نقل الطاولات إلى:
  - **Aljadreia**: 5 طاولات (1, 2, 3, 388, 496)
  - **Alsaydaia**: 3 طاولات (4, 5, 99)

#### 3. إخفاء الفروع الافتراضية
- **تأكيد:** API `/branches` و `/customer/menu` يخفيان الفروع الافتراضية

#### 4. عرض الشعار
- **تأكيد:** الشعار يظهر بشكل صحيح في تطبيق الزبائن

#### 5. زر البحث في التقارير
- **تأكيد:** زر "بحث" موجود ويعمل

#### نتائج الاختبار - Iteration 47:
- **Backend**: 100%
- **Frontend**: 100%

---

## آخر التحديثات (8 فبراير 2026) - الجزء 24

### ✅ إصلاحات شاملة للنظام

#### 1. إخفاء الفروع الافتراضية
- **المشكلة:** الفروع تعرض "الفرع الرئيسي" و"الفرع الثاني"
- **الإصلاح:** تعديل API `/branches` و `/customer/menu` لإخفاء الفروع الافتراضية
- **الملف:** server.py (سطور 2094-2096، 12306-12316)

#### 2. إضافة زر البحث في التقارير
- **المشكلة:** التقارير تحتاج زر بحث صريح
- **الإصلاح:** إضافة زر "بحث" مع أيقونة Search
- **الملف:** Reports.js (سطور 311-326)
- **ملاحظة:** التقارير فارغة لأنه لا توجد طلبات - هذا متوقع

#### 3. تطبيق الزبائن - اختيار الفروع
- **المشكلة:** التطبيق يذهب للقائمة مباشرة بدون اختيار الفرع
- **الإصلاح:** تعديل منطق `loadSavedData` و `fetchData` ليعرض اختيار الفروع أولاً
- **الملف:** CustomerMenu.js

#### 4. عرض الشعار
- **المشكلة:** الشعار يظهر كمربع برتقالي
- **الإصلاح:** عرض الحرف الأول من اسم المطعم إذا لم يكن هناك شعار
- **الملف:** CustomerMenu.js (سطور 546-566)

#### نتائج الاختبار - Iteration 46:
- **Backend**: 100%
- **Frontend**: 100%

#### ملاحظات:
- الطاولات مرتبطة بالفروع القديمة - يحتاج المستخدم لإنشاء طاولات للفروع الجديدة
- السائقين لا يوجدون لأن المستخدم لم يُضفهم بعد

---

## آخر التحديثات (28 يناير 2026) - الجزء 23

### ✅ إصلاح عرض وحفظ شعار المطعم

#### المشكلة:
شعار المطعم لا يُحفظ ولا يظهر دائماً في صفحة الإعدادات

#### الإصلاحات:
| الموقع | الإصلاح |
|--------|---------|
| Settings.js (1493-1512) | إضافة onError handler للصورة لمنع عرض صور مكسورة |
| Settings.js (1544-1574) | إصلاح تحديث الـ state بعد الحفظ ليُظهر الـ URL الصحيح |

#### نتائج الاختبار - Iteration 45:
- **Backend**: 100%
- **Frontend**: 100%
- الشعار يُعرض بشكل صحيح عند التحميل
- الشعار يُظهر معاينة فورية عند الاختيار
- الشعار يُحدّث للـ URL الجديد بعد الحفظ
- الشعار يظل ظاهراً بعد إعادة تحميل الصفحة

---

## آخر التحديثات (27 يناير 2026) - الجزء 22

### ✅ إصلاح صلاحيات المالك في إعدادات المطعم

#### المشكلة:
المالك (owner@maestroegp.com) يحاول تعديل إعدادات المطعم (الاسم والشعار) ويظهر له "غير مصرح"

#### السبب:
APIs كانت تتحقق من `UserRole.ADMIN` فقط بينما المالك لديه `UserRole.SUPER_ADMIN`

#### الإصلاح:
| الملف | السطر | التغيير |
|-------|-------|---------|
| server.py | 5724 | تغيير من `if role != ADMIN` إلى `if role not in [ADMIN, SUPER_ADMIN]` |
| server.py | 6819 | نفس التغيير لـ `/upload/restaurant-logo` |

#### نتائج الاختبار - Iteration 44:
- **Backend**: 100% (5/5 اختبار نجح)
- **Frontend**: 100%

---

## آخر التحديثات (27 يناير 2026) - الجزء 21

### ✅ إصلاح خطأ "غير مصرح" في إعدادات المطعم

#### المشكلة:
عند تعديل اسم المطعم ورفع الشعار كعميل (demo)، كان يظهر خطأ "غير مصرح"

#### السبب:
axios لا يرسل Authorization header تلقائياً مع طلبات multipart/form-data

#### الإصلاح:
| الملف | الإصلاح |
|-------|---------|
| Settings.js | إضافة `Authorization: Bearer ${token}` لطلب رفع شعار المطعم |
| Settings.js | إضافة `Authorization: Bearer ${token}` لطلب رفع صورة QR |

#### نتائج الاختبار - Iteration 43:
- **Backend**: 100% (5/5 اختبار نجح)
- **Frontend**: 100%

---

## آخر التحديثات (27 يناير 2026) - الجزء 20

### ✅ إضافة زر القص في جميع الأماكن

#### أماكن زر "قص وتعديل":
| الموقع | نسبة القص | العنوان |
|--------|----------|---------|
| هوية النظام | 1:1 (مربع) | قص شعار النظام |
| شعار الفواتير | 1:1 (مربع) | قص شعار الفواتير |
| شعار صفحة الدخول | 1:1 (مربع) | قص شعار صفحة الدخول |
| إضافة خلفية | 16:9 (عريض) | قص خلفية صفحة الدخول |

#### نتائج الاختبار - Iteration 42:
- **Frontend**: 100% نجاح

---

## آخر التحديثات (27 يناير 2026) - الجزء 19

### ✅ ميزة معاينة وقص الصور

#### الملف الجديد:
`/app/frontend/src/components/ImageCropper.js`

#### الميزات:
| الميزة | الوصف |
|--------|------|
| قص الصورة | تحديد منطقة القص بالسحب |
| التدوير | تدوير 90° في كل ضغطة |
| التكبير/التصغير | شريط تمرير من 50% إلى 200% |
| إعادة الضبط | إرجاع الصورة للوضع الأصلي |
| معاينة | عرض الصورة قبل الرفع |
| صورة أخرى | اختيار صورة جديدة |

#### مكان الاستخدام:
- 🔵 **هوية النظام** > زر "قص وتعديل"
- 🔵 **شعار الفواتير** > زر "قص وتعديل"

#### نتائج الاختبار - Iteration 41:
- **Frontend**: 100% نجاح
- **Backend**: 100% نجاح

---

## آخر التحديثات (27 يناير 2026) - الجزء 18

### ✅ إصلاح شامل لوظائف رفع الصور

#### المشاكل التي تم إصلاحها:
1. **مشكلة Authorization header**: كانت دوال رفع الصور ترسل طلبات بدون token
2. **زر رفع شعار صفحة الدخول**: كان معطلاً ولم يكن هناك input file

#### الإصلاحات المطبقة:
| الدالة | الملف | الإصلاح |
|--------|------|---------|
| `uploadSystemLogo` | SuperAdmin.js | إضافة Authorization header |
| `uploadLoginLogo` | SuperAdmin.js | إضافة Authorization header |
| `uploadBackgroundFromDevice` | SuperAdmin.js | إضافة Authorization header |
| `uploadTenantLogo` | SuperAdmin.js | إضافة Authorization header |
| `addNewBackground` | SuperAdmin.js | إضافة Authorization header |
| Login Page Logo | SuperAdmin.js | إضافة file input وزر تأكيد الرفع |

#### نتائج الاختبار - Iteration 40:
- **Backend**: 100% (13/13 اختبار نجح)
- **Frontend**: 100%
- جميع وظائف الرفع تعمل بشكل صحيح الآن

---

## آخر التحديثات (27 يناير 2026) - الجزء 17

### ✅ إضافة باقات اشتراك جديدة

#### الباقات المضافة:
| الباقة | السعر الافتراضي | الرمز |
|--------|----------------|-------|
| 🥉 برونزية | $15/شهر | bronze |
| 🥈 فضية | $30/شهر | silver |
| 🥇 ذهبية | $50/شهر | gold |

#### الميزات:
- ✅ المالك يحدد أسعار كل باقة يدوياً من لوحة التحكم
- ✅ المالك يفعّل الميزات لكل عميل يدوياً
- ✅ جميع الباقات تظهر في نموذج إنشاء/تعديل العميل
- ✅ نافذة الأسعار تعرض جميع الباقات بتصميم ملون جذاب

### ✅ تحديث إعدادات الإشعارات

#### الافتراضيات الجديدة:
- **التنبيه قبل انتهاء الاشتراك**: 15 يوم (بدلاً من 7)
- **إشعارات البريد الإلكتروني**: معطلة افتراضياً
- **إشعارات المتصفح**: مفعّلة

#### نتائج الاختبار - Iteration 39:
- **Backend**: 100% (12/12 اختبار نجح)
- **Frontend**: 100%

---

## آخر التحديثات (27 يناير 2026) - الجزء 16

### ✅ إصلاح مشكلة رفع الصور

#### المشكلة
- المستخدم أبلغ عن عدم عمل أزرار رفع الصور في النظام (شعار النظام، خلفيات صفحة الدخول، صور المنتجات والفئات)

#### السبب
- وجود تكرار في تعريف المتغيرات `systemLogoFile` و `systemLogoPreview` في ملف `SuperAdmin.js`
- التكرار كان في السطور 136-137 و 304-305

#### الإصلاح
- تم حذف التكرار الثاني (السطور 304-305)
- ملف: `/app/frontend/src/pages/SuperAdmin.js`

#### نتائج الاختبار - Iteration 38
- **Backend**: 100% (14/14 اختبار نجح)
  - ✅ `/api/upload/logo` - رفع شعار النظام
  - ✅ `/api/upload/image` - رفع صور المنتجات والفئات
  - ✅ `/api/upload/background` - رفع خلفيات صفحة الدخول
- **Frontend**: 100%
  - ✅ زر "رفع شعار" في الهوية البصرية يعمل
  - ✅ زر "إضافة خلفية" في صفحة الدخول يعمل
  - ✅ رفع صور المنتجات والفئات يعمل عبر ImageUploader

#### ملاحظة مهمة
- وكيل الاختبار لم يتمكن من إعادة إنتاج المشكلة بعد الإصلاح
- جميع وظائف الرفع تعمل بشكل صحيح الآن

---

## آخر التحديثات (26 يناير 2026) - الجزء 15

### ✅ التحكم بأسعار الاشتراكات بالدولار

#### 1. واجهة تعديل الأسعار
- **زر "تعديل الأسعار"** في قسم توزيع الاشتراكات
- **نافذة منبثقة** لتعديل الأسعار بالدولار

#### 2. الحقول المتاحة
| الحقل | الوصف | الافتراضي |
|-------|-------|-----------|
| الاشتراك الأساسي | السعر الشهري بالدولار | $25 |
| الاشتراك المميز | السعر الشهري بالدولار | $50 |

#### 3. ملخص الأسعار
- يُعرض السعر السنوي تلقائياً (الشهري × 12)
- مثال: أساسي $35/شهر = $420/سنة

#### 4. APIs الأسعار
```
GET  /api/super-admin/subscription-prices
PUT  /api/super-admin/subscription-prices
     Body: { "basic": 35, "premium": 75 }
```

#### 5. تحديثات العرض
- جميع الأسعار تُعرض بالدولار ($)
- الإيراد الشهري المتوقع بالدولار
- أسعار كل نوع اشتراك تحت اسمه

### ✅ نتائج الاختبار
- API جلب الأسعار: ✅
- API تحديث الأسعار: ✅
- نافذة تعديل الأسعار: ✅
- حفظ الأسعار في قاعدة البيانات: ✅
- تحديث العرض بعد الحفظ: ✅

---

## آخر التحديثات (26 يناير 2026) - الجزء 14

### ✅ لوحة معلومات الاشتراكات

#### 1. تبويب جديد "الاشتراكات"
- **موقعه:** لوحة تحكم المالك > إدارة العملاء > الاشتراكات

#### 2. بطاقات الملخص (4 بطاقات)
| البطاقة | الوصف | اللون |
|---------|-------|-------|
| اشتراك نشط | عدد الاشتراكات النشطة | أخضر |
| قارب على الانتهاء | الاشتراكات القريبة من الانتهاء | برتقالي |
| اشتراك منتهي | الاشتراكات المنتهية فعلاً | أحمر |
| إيراد شهري متوقع | الإيراد الشهري المتوقع | بنفسجي |

#### 3. قسم "قريبة من الانتهاء"
- يعرض الاشتراكات التي ستنتهي خلال X يوم (حسب الإعدادات)
- يعرض اسم العميل، البريد، وعدد الأيام المتبقية
- تنبيه أحمر إذا كان أقل من 3 أيام

#### 4. قسم "اشتراكات منتهية"
- يعرض الاشتراكات المنتهية فعلاً
- يعرض عدد الأيام منذ الانتهاء

#### 5. الإيرادات المتوقعة من التجديدات
- يُظهر المبلغ الإجمالي المتوقع من تجديد الاشتراكات القريبة من الانتهاء
- تفاصيل كل عميل مع نوع الاشتراك والمدة والمبلغ

#### 6. توزيع أنواع الاشتراكات
- يعرض إحصائيات كل نوع (أساسي، مميز، تجريبي، عرض)
- عدد الاشتراكات النشطة والمنتهية لكل نوع

#### 7. أسعار الاشتراكات الافتراضية
| النوع | السعر الشهري |
|-------|-------------|
| أساسي | 50,000 د.ع |
| مميز | 100,000 د.ع |
| تجريبي | 0 د.ع |
| عرض | 0 د.ع |

### ✅ نتائج الاختبار - Iteration 37
- Backend: 100% (12/12 اختبار)
- Frontend: 100%

---

## آخر التحديثات (26 يناير 2026) - الجزء 13

### ✅ نظام الإشعارات الفورية للمالك

#### 1. زر الإشعارات في الهيدر
- **أيقونة الجرس** مع عداد للإشعارات غير المقروءة
- **قائمة منسدلة** تعرض جميع الإشعارات
- **ميزات القائمة:**
  - تعليم الإشعار كمقروء بالنقر
  - حذف إشعار فردي
  - تعليم الكل كمقروء
  - حذف جميع الإشعارات

#### 2. نافذة إعدادات الإشعارات
- **التنبيه قبل انتهاء الاشتراك:**
  - قبل 3 أيام
  - قبل أسبوع (افتراضي)
  - قبل أسبوعين
  - قبل شهر
- **أنواع الإشعارات:**
  - إشعارات البريد الإلكتروني ✓
  - إشعارات المتصفح (Push) ✓
- **أحداث الإشعارات:**
  - إشعار عند إضافة عميل جديد ✓
  - إشعار عند تفعيل/تعطيل عميل ✓

#### 3. حقل مدة الاشتراك
- **يظهر فقط** للاشتراكات غير التجريبية (basic/premium)
- **خيارات المدة:**
  - شهر واحد
  - 3 أشهر
  - 6 أشهر
  - سنة كاملة

#### 4. الإشعارات التلقائية
| الحدث | عنوان الإشعار |
|-------|---------------|
| إضافة عميل جديد | عميل جديد 🎉 |
| تفعيل عميل | تم تفعيل عميل ✅ |
| تعطيل عميل | تم تعطيل عميل ⚠️ |

#### 5. APIs الإشعارات
```
GET  /api/super-admin/notifications
PUT  /api/super-admin/notifications/{id}/read
PUT  /api/super-admin/notifications/read-all
DELETE /api/super-admin/notifications/{id}
DELETE /api/super-admin/notifications
GET  /api/super-admin/notification-settings
PUT  /api/super-admin/notification-settings
GET  /api/super-admin/expiring-subscriptions
```

### ✅ نتائج الاختبار - Iteration 36
- Backend: 100% (13/13 اختبار)
- Frontend: 100% (جميع UI components)

---

## آخر التحديثات (26 يناير 2026) - الجزء 12

### ✅ إصلاح وظيفة "رفع خلفية جديدة"
- **تم إضافة Dialog جديد** لإضافة خلفيات صفحة الدخول
- **وضعين للإضافة:**
  - **رابط URL** - إدخال رابط الصورة مباشرة
  - **رفع من الجهاز** - اختيار صورة من الجهاز (PNG, JPG, WEBP, GIF حتى 5MB)
- **خيارات إضافية:**
  - عنوان الخلفية (اختياري)
  - نوع الحركة (Fade/Zoom/Ken Burns/Slide)

### ✅ التحقق من نافذة الميزات والصلاحيات
- **4 أقسام:**
  - 🟢 الميزات الأساسية (8): نقاط البيع، الطاولات، الطلبات، التقارير، المصاريف، المخزون، التوصيل، شاشة المطبخ
  - 🟣 الميزات المتقدمة (8): الموارد البشرية، التحويلات، كول سنتر، سجل المكالمات، برامج الولاء، الكوبونات، الوصفات، حجوزات الطاولات
  - 🟡 ميزات إضافية (4): التقارير الذكية، طلبات الفروع، المشتريات، التقييمات
  - 🔵 خيارات الإعدادات (9): المستخدمين، العملاء، الفئات، المنتجات، شركات التوصيل، الفروع، المطبوعات، الإشعارات، كول سنتر
- **أزرار "تفعيل الكل" و "تعطيل الكل"**
- **زر "حفظ الميزات"**

### ✅ التحقق من قائمة مدة الانتقال
- **8 خيارات:** 0.5، 1، 1.5، 2، 2.5، 3، 4، 5 ثواني
- **تعمل بشكل صحيح** في إعدادات صفحة الدخول

### ✅ التحقق من استبعاد الحسابات التجريبية
- **إحصائيات المالك** تستبعد الحسابات التجريبية من:
  - إجمالي العملاء
  - إجمالي المستخدمين
  - إجمالي الطلبات
  - إجمالي المبيعات
- **عدد الحسابات التجريبية** يُعرض بشكل منفصل

### ✅ توضيح آلية packaging_cost
- **يُضاف فقط** لطلبات التوصيل (delivery) والسفري (takeaway)
- **لا يُضاف** لطلبات الجلوس (dine_in)
- **يُحسب** لكل منتج ويُضرب بالكمية
- **يُحفظ** في الطلب النهائي ضمن إجمالي التكلفة

### ✅ نتائج الاختبار - Iteration 35
- Backend: 100% (14/14 اختبار)
- Frontend: 100% (جميع تدفقات UI)

---

## آخر التحديثات (26 يناير 2026) - الجزء 11

### ✅ إصلاح مشاكل بيانات المالك (Owner/Super Admin)

#### المشاكل التي تم إصلاحها:
1. **وظيفة "الدخول كعميل"** - كان يظهر خطأ `KeyError: 'full_name'`
   - **السبب:** بعض المستخدمين لديهم حقل `name` وليس `full_name`
   - **الحل:** استخدام `admin.get("full_name") or admin.get("name", "")`
   - **الملف:** `server.py` - دالة `impersonate_tenant`

2. **عرض الفروع للمالك** - كان يُرجع قائمة فارغة `[]`
   - **السبب:** الكود كان يعيد `[]` مباشرة للـ `super_admin`
   - **الحل:** تعديل الدالة لجلب الفروع بـ `tenant_id: default` للمالك
   - **الملف:** `server.py` - دالة `get_branches`

3. **عرض التصنيفات للمالك** - نفس المشكلة
   - **الحل:** نفس التعديل
   - **الملف:** `server.py` - دالة `get_categories`

4. **عرض المنتجات للمالك** - نفس المشكلة
   - **الحل:** نفس التعديل
   - **الملف:** `server.py` - دالة `get_products`

#### نتائج الاختبار - Iteration 34:
- ✅ 100% نجاح (18/18 اختبار backend)
- ✅ 100% نجاح UI tests
- ✅ المالك يرى 6 فروع، 9 تصنيفات، 8+ منتجات
- ✅ وظيفة "الدخول كعميل" تعمل بنجاح

#### البيانات الحالية للمالك:
| النوع | العدد | الأمثلة |
|-------|-------|---------|
| الفروع | 6 | الفرع الرئيسي، Aljadreia، Alsaydaia، فرع المالك الرئيسي |
| التصنيفات | 9 | المشروبات، الوجبات الرئيسية، الحلويات، برغر، بيتزا |
| المنتجات | 8+ | برغر كلاسيك، برغر دبل، كولا، شاي، قهوة عربية |

---

## آخر التحديثات (26 يناير 2026) - الجزء 10

### ✅ الهوية البصرية للنظام (Visual Identity)
- **تم تغيير اسم الزر** من "إعدادات الفواتير" إلى "**الهوية البصرية**"
- **نافذة شاملة** مع 3 تبويبات:
  - 👑 **هوية النظام** - اسم النظام، الشعار
  - 🧾 **إعدادات الفواتير** - بيانات الاتصال، محتوى الفاتورة، معاينة
  - 🎨 **صفحة الدخول** - خلفيات، شعار، ألوان

### ✅ تحسينات صفحة الدخول
- **الخلفيات تظهر كصور كاملة** (aspect-video) مع:
  - اسم الخلفية
  - نوع الحركة (fade/zoom/kenburns)
  - علامة "مفعّل/معطّل"
  - أزرار تعطيل/حذف عند التحويم
- **قائمة مدة الانتقال المنسدلة** (0.5 - 5 ثواني)
- **Color Picker** لألوان الغطاء والنص

### ✅ تفعيل تبويب المستخدمين للـ Owner
- **إصلاح Settings.js** - أصبح تبويب "المستخدمين والموظفين" متاحاً للـ `super_admin` و `admin`

### ✅ إضافة بيانات للحساب التجريبي
- **فرعين جديدين**:
  - Aljadreia - الجادرية، بغداد
  - Alsaydaia - الصيدلية، بغداد
- **منتجات جديدة**: شاي، قهوة عربية، برجر لحم، شاورما دجاج، كنافة

---

## آخر التحديثات (26 يناير 2026) - الجزء 9

### ✅ نافذة الميزات والصلاحيات - التصميم الكامل
- **تم إصلاح imports الأيقونات المفقودة** في `SuperAdmin.js`
- **نافذة الميزات تعرض 4 أقسام**:
  - 🟢 **الميزات الأساسية** (8): نقاط البيع، الطاولات، الطلبات، التقارير، المصاريف، المخزون، التوصيل، شاشة المطبخ
  - 🟣 **الميزات المتقدمة** (8): الموارد البشرية، التحويلات، كول سنتر، سجل المكالمات، برامج الولاء، الكوبونات، الوصفات، حجوزات الطاولات
  - 🟡 **ميزات إضافية** (4): التقارير الذكية، طلبات الفروع، المشتريات، التقييمات
  - 🔵 **خيارات الإعدادات** (9): المستخدمين، العملاء، الفئات، المنتجات، شركات التوصيل، الفروع، المطبوعات، الإشعارات، كول سنتر
- **أزرار التحكم الشامل**: تفعيل الكل / تعطيل الكل
- **زر الحفظ** يحفظ جميع الصلاحيات بنجاح

### ✅ نتائج الاختبار - Iteration 33
- 100% نجاح (14/14 اختبار backend، جميع اختبارات UI)
- API: `GET /api/super-admin/tenants/{id}/features` ✅
- API: `PUT /api/super-admin/tenants/{id}/features` ✅
- واجهة المستخدم: جميع أزرار Switch تعمل ✅

### ✅ بداية إعادة الهيكلة
- **تم إنشاء مجلد `/app/backend/models/`** مع تقسيم النماذج:
  - `base_models.py` - الثوابت والنماذج الأساسية
  - `user_models.py` - نماذج المستخدمين
  - `tenant_models.py` - نماذج العملاء
  - `order_models.py` - نماذج الطلبات
  - `inventory_models.py` - نماذج المخزون
  - `hr_models.py` - نماذج الموارد البشرية

---

## آخر التحديثات (26 يناير 2026) - الجزء 8

### ✅ إصلاح هيكل النظام بالكامل
- **حذف مفهوم "النظام الرئيسي" كعميل** - لا يوجد عميل مرتبط بـ admin@maestroegp.com
- **لوحة تحكم المالك هي النظام الرئيسي** - المالك يدير كل شيء من `/super-admin`
- **تبويبات لفصل العملاء**:
  - العملاء الفعليين (2): Hosam Food, GRaffiti burger
  - الحسابات التجريبية (1): نظام تجريبي
  - الكل (3)

### ✅ إنشاء الحساب التجريبي
- **بيانات الدخول**: `demo@maestroegp.com` / `demo123`
- **الفروع**: الفرع الرئيسي، الفرع الثاني
- **الفئات**: المشروبات، الوجبات الرئيسية، المقبلات، الحلويات
- **المنتجات**: 13 منتج متنوع (عصائر، برجر، شاورما، حمص، كنافة، إلخ)
- **الطاولات**: 5 طاولات (3 داخلي، 2 خارجي)

### ✅ الهيكل الصحيح الآن
| المستخدم | الدور | الوصف |
|----------|-------|-------|
| `owner@maestroegp.com` | super_admin | مالك النظام - لوحة تحكم المالك `/super-admin` |
| `demo@maestroegp.com` | admin | حساب تجريبي للعرض |
| `hanialdujaili@gmail.com` | admin | عميل GRaffiti |
| `hosam@hosamfood.com` | admin | عميل Hosam Food |

---

## آخر التحديثات (26 يناير 2026) - الجزء 6-7

### ✅ تحسينات إضافية على فصل الفروع
- **إضافة badge لعدد الطلبات المعلقة:** يظهر على BranchSelector
- **تحديث أسماء الفروع:** Aljadraia و Alsaydaia
- **إخفاء قسم المستخدمين للـ super_admin:** المالك يدير المستخدمين من لوحة التحكم فقط
- **عزل قائمة المستخدمين:** كل admin يرى فقط مستخدمي مطعمه

---

## آخر التحديثات (26 يناير 2026) - الجزء 5

### ✅ فصل البيانات بين الفروع (Multi-Branch Isolation)
- **مكون اختيار الفرع في POS:** أضيف BranchSelector في رأس صفحة نقاط البيع
- **تحديث البيانات تلقائياً:** عند تغيير الفرع، تتحدث الطاولات والسائقين والطلبات المعلقة
- **فلترة حسب الفرع:**
  - `/api/tables?branch_id=X` - الطاولات
  - `/api/orders?branch_id=X` - الطلبات
  - `/api/drivers?branch_id=X` - السائقين
- **إنشاء الطلبات:** يُستخدم الفرع المحدد عند إنشاء طلب جديد
- **نتائج الاختبار:** 100% نجاح (15/15 اختبار) - Iteration 32

### ✅ الملفات المُعدّلة
- `/app/frontend/src/pages/POS.js` - إضافة BranchSelector وتحديث fetchData
- استخدام `useBranch()` و `getBranchIdForApi()` للحصول على الفرع النشط

---

## آخر التحديثات (26 يناير 2026) - الجزء 4

### ✅ إصلاح هيكل النظام
- **حذف أحمد نهائياً** مع جميع بياناته
- **إضافة السوبر أدمن**: admin@maestroegp.com / admin123 / مفتاح السر: 271018
- **إصلاح الفروع**: كل عميل لديه Aljadraia, Alsaydaia
- **حذف "الفرع الرئيسي"** من جميع العملاء
- **فصل بيانات العملاء** بشكل كامل

### ✅ العملاء الحاليين
- **GRaffiti burger**: hanialdujaili@gmail.com / graffiti123
- **Hosam Food**: hosam@hosamfood.com

### ✅ نتائج الاختبار
- 100% نجاح (10/10 اختبارات)

---

## آخر التحديثات (26 يناير 2026) - الجزء 3

### ✅ إصلاح تكرار الفئات والطاولات
- حذف الفئات المكررة للعملاء
- حذف جميع الطاولات القديمة وإنشائها بشكل صحيح
- الآن كل عميل لديه طاولات منفصلة

### ✅ إضافة ميزة حذف الطاولات
- زر حذف يظهر لكل طاولة متاحة
- نافذة تأكيد قبل الحذف
- الحذف النهائي من قاعدة البيانات

### ✅ تصفية الطاولات حسب القسم في POS
- أزرار لتصفية الطاولات: الكل، داخلي، خارجي، VIP، تراس، إلخ
- عرض عدد الطاولات في كل قسم
- الأقسام ديناميكية حسب الطاولات الموجودة

### ✅ أقسام الطاولات
- إضافة حقل "القسم" عند إنشاء طاولة
- أقسام افتراضية: داخلي، خارجي، تراس، بلكون، طابق أرضي، VIP
- إمكانية كتابة قسم مخصص

### ✅ إصلاح الفروع
- حذف الفروع القديمة
- إنشاء فرع لكل عميل باسم مطعمه

### ✅ الحذف النهائي
- حذف العميل نهائياً مع جميع بياناته عند استخدام `?permanent=true`
- حذف الطاولات نهائياً

### ✅ إصلاح صلاحيات super_admin
- إضافة super_admin للأدوار المسموح لها بإنشاء وحذف الطاولات

---

## آخر التحديثات (26 يناير 2026) - الجزء 2

### ✅ فصل مبيعات النظام الرئيسي عن مبيعات العملاء
- تم تحديث `/api/super-admin/stats` لاستبعاد طلبات ومبيعات النظام الرئيسي (tenant_id = "default")
- إحصائيات المالك تعرض الآن فقط مبيعات وطلبات العملاء الفعليين

### ✅ إصلاح مشكلة الطاولات
- تم إنشاء 5 طاولات افتراضية لكل عميل جديد
- إصلاح مشكلة عدم ظهور الطاولات في إدارة الطاولات
- تنظيف الطاولات المكررة

### ✅ إضافة صور للفئات والمنتجات
- إضافة صور Unsplash للفئات الافتراضية (برغر، بيتزا، مشروبات، حلويات، سلطات)
- إضافة صور للمنتجات الافتراضية
- تنظيف المنتجات المكررة

### ✅ تحسين إعدادات الفاتورة
- إضافة حقل `system_name` (اسم النظام) في إعدادات الفاتورة
- الآن يظهر في أسفل الفاتورة: اسم النظام، الأرقام، البريد، الموقع

### ✅ إصلاح تفعيل/تعطيل العملاء
- إضافة نقطة نهاية `/api/super-admin/tenants/{id}/reactivate`
- تحديث المستخدمين تلقائياً عند تفعيل/تعطيل العميل

### ✅ إضافة نقطة نهاية لإصلاح البيانات
- `/api/fix-data` للمالك لإصلاح البيانات القديمة (الطاولات، الصور)

---

## آخر التحديثات (26 يناير 2026)

### ✅ إعادة هيكلة الفاتورة بالكامل

**الهيكل الجديد للفاتورة:**

1. **أعلى الفاتورة - معلومات المطعم (يتحكم به العميل):**
   - اسم المطعم
   - عنوان المطعم
   - أرقام هواتف المطعم
   - الرقم الضريبي (مع خيار إظهار/إخفاء)

2. **وسط الفاتورة:**
   - التاريخ والوقت ورقم الفاتورة
   - نص مخصص أعلى الفاتورة
   - جدول الأصناف والأسعار
   - المجموع والإجمالي
   - نص مخصص أسفل من المطعم

3. **أسفل الفاتورة - معلومات النظام (يتحكم به المالك):**
   - شعار النظام
   - رسالة الشكر
   - أرقام التواصل مع النظام
   - البريد والموقع الإلكتروني

### ✅ فصل صلاحيات الشعار

**المالك (Super Admin):**
- رفع شعار النظام الذي يظهر في جميع الفواتير إجبارياً
- التحكم في رسالة الشكر وأرقام النظام
- إظهار/إخفاء معلومات النظام من الفواتير

**العميل (صاحب المطعم):**
- اسم المطعم والعنوان
- أرقام هاتف المطعم
- الرقم الضريبي مع خيار إظهار/إخفاء
- نصوص مخصصة في الفاتورة

### ✅ إصلاح طباعة الفاتورة

- CSS جديد يُظهر الفاتورة فقط عند الطباعة
- إخفاء خلفية النظام وكل العناصر الأخرى
- عرض 80mm مناسب للطابعات الحرارية

---

## آخر التحديثات (25 يناير 2026) - الجزء 2

### ✅ تحسينات معاينة وطباعة الفاتورة
**تم إصلاح مشكلة طباعة الفاتورة وإضافة تحسينات:**

1. **CSS طباعة مُحسّن:**
   - إضافة class `print-receipt` لإظهار الفاتورة فقط عند الطباعة
   - عرض الفاتورة 80mm مناسب للطابعات الحرارية
   - إخفاء خلفية النظام والأزرار عند الطباعة

2. **معاينة الفاتورة الكاملة:**
   - عرض شعار المطعم/الفاتورة
   - عرض اسم المطعم والعنوان
   - نص أعلى وأسفل الفاتورة (مخصص)
   - أرقام الهواتف والرقم الضريبي
   - جدول الأصناف مع الأسعار والمجاميع

3. **رفع شعار الفاتورة:**
   - إضافة قسم "شعار الفاتورة" في إعدادات الفاتورة
   - زر رفع شعار جديد مع معاينة
   - زر إزالة الشعار
   - التراجع لشعار المطعم الافتراضي إذا لم يوجد شعار مخصص

### ✅ إصلاح صور المنتجات والفئات
- إصلاح معالجة روابط الصور (إضافة API prefix)
- إضافة fallback للصور المفقودة
- الآن تظهر أيقونة placeholder عند عدم وجود صورة

---

## آخر التحديثات (25 يناير 2026)

### ✅ تخصيص الفواتير للعميل - جديد!
**تم إضافة واجهة لإعدادات الفاتورة للعملاء (أصحاب المطاعم):**

#### المكونات المضافة:
1. **واجهة إعدادات الفاتورة (Settings > إعدادات الفاتورة):**
   - معلومات الاتصال (رقم الهاتف الأول والثاني)
   - عنوان المطعم
   - الرقم الضريبي
   - نص أعلى الفاتورة (مخصص)
   - نص أسفل الفاتورة (مخصص)
   - خيار إظهار/إخفاء شعار المطعم

2. **نقاط النهاية:**
   - `GET /api/tenant/invoice-settings` - جلب إعدادات الفاتورة للعميل
   - `PUT /api/tenant/invoice-settings` - تحديث إعدادات الفاتورة

### ✅ تفعيل صلاحيات الطابعات (المنطق الخلفي) - جديد!
**تم تنفيذ المنطق الخلفي لتطبيق صلاحيات الطابعة عند الطباعة:**

#### الصلاحيات المُفعّلة:
1. **`show_prices`** - إخفاء/إظهار الأسعار في الفاتورة
2. **`print_mode`** - وضع الطباعة:
   - `full_receipt`: فاتورة كاملة (مع المعلومات المالية)
   - `orders_only`: طلبات فقط (بدون معلومات مالية)
3. **`print_individual_items`** - طباعة كل صنف على ورقة منفصلة

#### نقاط النهاية المُحدثة:
- `POST /api/invoices/print/{order_id}` - تطبيق صلاحيات الطابعة عند الطباعة
- `GET /api/invoices/auto-print/{order_id}` - جديد! جلب بيانات الطباعة لكل الطابعات النشطة

---

## آخر التحديثات (24 يناير 2026) - الجزء 4

### ✅ تقرير الإرجاعات
**تم إضافة تبويب "الإرجاعات" في صفحة التقارير:**
- إجمالي مبالغ الإرجاعات
- عدد الإرجاعات
- متوسط قيمة الإرجاع
- جدول تفصيلي يشمل:
  - رقم الفاتورة
  - نوع الطلب (محلي/سفري/توصيل)
  - القيمة الأصلية
  - قيمة الإرجاع
  - سبب الإرجاع
  - اسم من قام بالإرجاع
  - التاريخ والوقت

### ✅ دمج المستخدمين والموظفين
**تم دمج تبويب "المستخدمين" و "الأدوار والموظفين" في تبويب واحد:**
- التبويب الجديد: "المستخدمين والموظفين"
- يحتوي على تبويبات فرعية:
  - "مستخدمي النظام" - حسابات الدخول وصلاحياتها
  - "الموظفين" - إدارة الموظفين وأدوارهم

---

## آخر التحديثات (24 يناير 2026) - الجزء 3

### ✅ ميزة إرجاع الطلبات (Refund) - جديد!
**تم إضافة نظام متكامل لإرجاع الطلبات في نقاط البيع:**

#### المكونات المضافة:
1. **Backend (server.py):**
   - `POST /api/refunds` - إنشاء إرجاع جديد
   - `GET /api/refunds` - جلب قائمة الإرجاعات
   - `GET /api/refunds/{id}` - جلب تفاصيل إرجاع
   - `GET /api/orders/{id}/refund-status` - التحقق من حالة الإرجاع

2. **Frontend (POS.js):**
   - زر "إرجاع طلب" بجانب "الطلبات المعلقة"
   - حوار إرجاع الطلب مع:
     - البحث برقم الفاتورة
     - عرض معلومات الطلب وحالته
     - حقل سبب الإرجاع (إجباري)
     - تأكيد الإرجاع

3. **الصلاحيات (Settings.js):**
   - صلاحية جديدة `can_refund` - "إرجاع الطلبات"
   - المدير والمالك لديهم صلاحية تلقائية
   - يمكن تعيينها للكاشير حسب الحاجة

#### قواعد الإرجاع:
- يجب إدخال سبب الإرجاع (3 أحرف على الأقل)
- الطلب المدفوع فقط يمكن إرجاعه
- لا يمكن إرجاع نفس الطلب مرتين
- يتم تسجيل الإرجاع تلقائياً في المصاريف

---

## آخر التحديثات (24 يناير 2026) - الجزء 2

### ✅ إصلاح مشكلة الفروع المكررة
- تم حذف 5 فروع مكررة باسم "الفرع الرئيسي" من قاعدة البيانات
- تم تعديل endpoint `/api/branches` لإخفاء الفروع المعطّلة تلقائياً
- إضافة parameter اختياري `include_inactive=true` لعرض جميع الفروع

### ✅ مواد الفيديو الترويجي
- تم إنشاء سيناريو فيديو ترويجي كامل (60-90 ثانية)
- تم أخذ 7 لقطات شاشة عالية الجودة للصفحات الرئيسية
- الملف: `/app/PROMO_VIDEO_SCRIPT.md`

### ✅ جاهزية النشر
- تم فحص التطبيق بالكامل بواسطة وكيل النشر
- **النتيجة:** جاهز للنشر بدون مشاكل

---

## آخر التحديثات (24 يناير 2026) - الجزء 1

### ✅ ميزة التحكم بشعار صفحة تسجيل الدخول - جديد!
**تم إضافة أداة للمالك (Owner) للتحكم في شعار صفحة تسجيل الدخول:**

#### المكونات المضافة:
1. **نقاط نهاية جديدة في Backend:**
   - `POST /api/login-backgrounds/upload-logo` - رفع شعار من الجهاز
   - `PUT /api/login-backgrounds/logo-url` - تحديث الشعار برابط خارجي
   - `DELETE /api/login-backgrounds/logo` - حذف الشعار

2. **واجهة مستخدم في لوحة تحكم المالك (SuperAdmin):**
   - معاينة الشعار الحالي
   - خيار رفع صورة من الجهاز (PNG, JPG, SVG, WebP)
   - خيار إضافة رابط خارجي للشعار
   - زر حذف الشعار
   - تفعيل/إلغاء عرض الشعار

3. **تحديث صفحة تسجيل الدخول:**
   - دعم الشعارات المرفوعة محلياً والروابط الخارجية
   - الشعار يملأ المربع بالكامل بحواف دائرية
   - معالجة أخطاء تحميل الصور (fallback للحرف M)

#### الملفات المُعدلة:
- `/app/backend/server.py` - إضافة 3 نقاط نهاية جديدة
- `/app/frontend/src/pages/SuperAdmin.js` - واجهة التحكم بالشعار
- `/app/frontend/src/pages/Login.js` - دعم الشعارات المخصصة

---

## آخر التحديثات (23 يناير 2026) - الجزء 2

### ✅ إعادة هيكلة الكود - المرحلة الأولى مكتملة
**تم تقليص `server.py` من 13,697 إلى 11,549 سطر (-2,148 سطر)**

| الملف | الأسطر | الوظائف |
|-------|--------|---------|
| `shared.py` | 143 | الدوال المشتركة |
| `reports_routes.py` | 676 | 10 تقارير |
| `drivers_routes.py` | 423 | 15 نقطة نهاية |
| `payroll_routes.py` | 543 | 12 نقطة نهاية |
| `shifts_routes.py` | 564 | 8 نقاط نهاية |
| **المجموع** | **2,349** | تم نقلها |

### ✅ نتائج الاختبار (Iteration 25):
- **الواجهة الخلفية:** 20/20 اختبار (100%)
- **الواجهة الأمامية:** جميع الصفحات تعمل
- **لا يوجد تراجع** في الوظائف

### ⏳ الهدف المتبقي:
- تقليل `server.py` إلى أقل من 5,000 سطر (المرحلة الثانية)

---

## آخر التحديثات (23 يناير 2026) - الجزء 1

### ✅ إعادة هيكلة الكود الشاملة (Code Refactoring)
**تم نقل ~1,500 سطر من server.py إلى 4 ملفات منفصلة:**

#### 1. `shared.py` (~150 سطر) - الدوال المشتركة:
- اتصال قاعدة البيانات (singleton)
- Enums (UserRole, OrderStatus, PaymentMethod, etc.)
- دوال المصادقة (get_current_user, create_token)
- دوال بناء الاستعلامات (build_tenant_query, build_branch_query)

#### 2. `reports_routes.py` (~600 سطر) - التقارير:
- `/reports/sales` - تقرير المبيعات
- `/reports/purchases` - تقرير المشتريات
- `/reports/inventory` - تقرير المخزون
- `/reports/expenses` - تقرير المصروفات
- `/reports/profit-loss` - تقرير الأرباح والخسائر
- `/reports/delivery-credits` - تقرير ديون التوصيل
- `/reports/products` - تقرير المنتجات
- `/reports/cancellations` - تقرير الإلغاءات
- `/reports/discounts` - تقرير الخصومات
- `/reports/credit` - تقرير الآجل

#### 3. `drivers_routes.py` (~350 سطر) - السائقين:
- CRUD للسائقين (إنشاء، قراءة، تعديل، حذف)
- تعيين السائقين للطلبات
- إكمال التوصيل وتتبع GPS
- إحصائيات السائقين والدفعات
- بوابة السائق (Portal) بدون مصادقة

#### 4. `payroll_routes.py` (~400 سطر) - الرواتب:
- إدارة الخصومات والمكافآت
- حساب وكشوف الرواتب
- صرف الرواتب وتقارير ملخصة
- كشف راتب الموظف

### ✅ نتائج الاختبار الشامل (Iteration 24):
- **الواجهة الخلفية:** 26/26 اختبار ناجح (100%)
- **الواجهة الأمامية:** جميع الصفحات تعمل بدون أخطاء
- **لا يوجد أي تراجع (regression)** في الوظائف الأصلية

### ✅ ملفات التوثيق:
- `/app/backend/REFACTORING.md` - خطة إعادة الهيكلة والخطوات التالية

---

## آخر التحديثات (22 يناير 2026)

### ✅ نظام إشعارات الطلبات المتأخرة - جديد!
**الميزة:** نظام إشعارات ذكي لإعلام المدير عند تأخر الطلبات، يساعد في تحسين سرعة الخدمة.

**المكونات:**
1. **نقطة نهاية جديدة** `GET /api/notifications/delayed-orders`:
   - تحديد الطلبات المتأخرة (افتراضي: أكثر من 15 دقيقة)
   - تصنيف مستويات التأخير:
     - 🔴 حرج: +45 دقيقة
     - 🟠 عالي: +30 دقيقة
     - 🟡 متوسط: +15 دقيقة
   - إحصائيات: إجمالي، أقصى تأخير، متوسط التأخير

2. **تنبيه في لوحة التحكم:**
   - يظهر تلقائياً عند وجود طلبات متأخرة
   - لون التنبيه يتغير حسب خطورة التأخير
   - يتم التحقق كل 30 ثانية
   - صوت تنبيه خفيف للطلبات الحرجة/العالية
   - زر "عرض الطلبات" للانتقال مباشرة لصفحة الطلبات

**الملفات المُعدلة:**
- `/app/backend/server.py` - إضافة نقطة النهاية
- `/app/frontend/src/pages/Dashboard.js` - إضافة عرض التنبيه

---

### ✅ إصلاح مشكلة "خطأ الاتصال بالخادم" على النسخة المنشورة
**المشكلة:** عند نشر التطبيق على نطاق `.emergent.host`، كان يظهر خطأ "فشل الاتصال بالخادم" لأن الواجهة الأمامية كانت تستخدم رابط الـ preview الثابت.

**الحل المُنفذ:**
1. **إنشاء ملف مركزي** `/app/frontend/src/utils/api.js` يحدد رابط الـ API ديناميكياً:
   - في بيئة الإنتاج (`.emergent.host`): يستخدم `window.location.origin`
   - في بيئة المعاينة (`.emergentagent.com`): يستخدم `window.location.origin`
   - في بيئة التطوير: يستخدم `REACT_APP_BACKEND_URL` أو `localhost`

2. **تحديث جميع الملفات** لاستخدام الملف المركزي:
   - `/app/frontend/src/pages/CustomerMenu.js`
   - `/app/frontend/src/pages/SystemAdmin.js`
   - `/app/frontend/src/pages/Dashboard.js`
   - `/app/frontend/src/context/AuthContext.js`

**الإجراء المطلوب:** يجب الضغط على زر **"Deploy"** لنشر التحديثات.

---

### ✅ نظام صلاحيات القائمة الجانبية (الأزرار السريعة)
**الوضع الحالي:**
- نظام الصلاحيات مُنفذ ويعمل في `/app/frontend/src/pages/Dashboard.js`
- كل زر في الأزرار السريعة يتحقق من صلاحيات المستخدم
- المدير والـ Super Admin يرون جميع الأزرار
- مدير الفرع يرى جميع الأزرار
- الكاشير يرى فقط: نقاط البيع، الطاولات، المصاريف، التوصيل
- يمكن تخصيص الصلاحيات من صفحة الإعدادات > المستخدمين

---

### 🔄 إعادة هيكلة `server.py` (قيد التخطيط)
**الوضع الحالي:**
- ملف `server.py` يحتوي على **+13,000 سطر** و **321 نقطة نهاية**
- يوجد مجلد `/app/backend/routes/` مع بعض الملفات الجاهزة لكنها غير مفعلة بالكامل

**الملفات الموجودة في routes:**
- `inventory_system.py` (مُفعل)
- `auth.py`, `users.py`, `hr.py`, `branches.py`, `products.py` (جاهزة للاستخدام)

**الخطة المقترحة للهيكلة:**
1. نقل نقاط نهاية الطلبات إلى `routes/orders.py`
2. نقل نقاط نهاية السائقين إلى `routes/drivers.py`
3. نقل نقاط نهاية التقارير إلى `routes/reports.py`
4. تفعيل الملفات الموجودة في `routes/` تدريجياً

---

## التحديثات السابقة (21 يناير 2025)

### ✅ إصلاح مشكلة تثبيت تطبيق العملاء (PWA) - جديد!
**المشكلة:** عند تثبيت تطبيق العملاء من iOS Safari، كانت تظهر أيقونة واسم تطبيق السائق بدلاً من تطبيق العملاء.

**السبب:** iOS Safari يخزن أول manifest يجده للنطاق ويتجاهل الباقي عندما يكون `scope` واحد.

**الحل المُنفذ:**
1. **إنشاء مجلد منفصل `/customer-pwa/`** مع:
   - `manifest.json` خاص بتطبيق العملاء مع `scope: "/customer-pwa/"`
   - `index.html` صفحة تثبيت مخصصة للعملاء

2. **تحديث manifest السائق** بإضافة `scope: "/driver"` لفصله

3. **إنشاء Service Worker منفصل** (`customer-sw.js`) لتطبيق العملاء

4. **تحديث Dialog رابط القائمة في Dashboard:**
   - رابط القائمة العادي (للتصفح) + QR أسود
   - رابط تثبيت التطبيق (PWA) + QR برتقالي ⭐
   - الملفات: `/customer-pwa/manifest.json`, `/customer-pwa/index.html`, `/customer-sw.js`

**للاختبار:** يجب على المستخدم:
1. مسح ذاكرة التخزين المؤقت للموقع في Safari (الإعدادات > Safari > محو سجل التصفح والبيانات)
2. فتح الرابط: `{DOMAIN}/customer-pwa/`
3. إضافة للشاشة الرئيسية

---

### ✅ تعديلات التقييمات والمصاريف
**تم تنفيذ التعديلات التالية:**

1. **تقييم الموظفين:**
   - تغيير اسم "التقييمات" إلى "تقييم الموظفين" في لوحة التحكم
   
2. **تقييمات العملاء:**
   - إضافة تبويب "تقييمات العملاء" في صفحة برنامج الولاء
   - API: `GET /api/customer-reviews`, `POST /api/customer-reviews`
   - يعرض: اسم العميل، رقم الطلب، التقييم بالنجوم، التعليق، تقييمات مفصلة (طعام/خدمة/سرعة)

3. **حقل غاز في المصاريف:**
   - إضافة تصنيف "غاز" 🔥 في قائمة المصاريف

### ✅ إعدادات المطعم الجديدة
**تم إضافة تاب "المطعم" في صفحة الإعدادات:**
- تغيير اسم المطعم (عربي وإنجليزي)
- رفع شعار المطعم
- الاسم والشعار يظهران في تطبيق العملاء تلقائياً
- **API:** `PUT /api/settings/restaurant`, `GET /api/settings/restaurant`

### ✅ إصلاح تطبيق العملاء (قائمة الطعام)
**تم إصلاح 4 مشاكل رئيسية:**

1. **QR Code يعمل بشكل صحيح:**
   - إضافة مكتبة `qrcode.react` لتوليد QR Code حقيقي
   - رمز QR قابل للمسح ويوجه للرابط الصحيح

2. **شعار المطعم:**
   - يعرض شعار المطعم إذا تم رفعه من الإعدادات
   - Fallback: حرف M برتقالي على خلفية سوداء إذا لم يوجد شعار

3. **أيقونة تثبيت التطبيق:**
   - خلفية سوداء مع حرف M برتقالي (#f97316)
   - manifest-customer.json منفصل لتطبيق العملاء
   - أيقونات جديدة: customer-icon-192.png و customer-icon-512.png

4. **فصل تطبيق العملاء عن بوابة السائقين:**
   - manifest مختلف لكل تطبيق
   - تحديث index.html للتعامل مع المسارات المختلفة
   - `/menu/*` -> تطبيق العملاء
   - `/driver/*` -> بوابة السائقين

### ✅ ميزة تصفير بيانات المخزون (للمالك فقط)
**تم نقل وظيفة تصفير المخزون للوحة تحكم المالك فقط:**
- أيقونة جديدة 📦 (بنفسجية) بجانب أيقونة تصفير المبيعات في قائمة العملاء
- **API:** `POST /api/super-admin/tenants/{tenant_id}/reset-inventory?confirm=true`
- يحذف:
  - طلبات الفروع
  - فواتير الشراء
  - سجلات التصنيع
  - حركات المخزون
  - يصفر كميات المواد الخام والمنتجات المصنعة
  - مخزون الفروع

**ملاحظة:** تم إزالة قسم التصفير من نافذة "إدارة اليوم" للعميل - فقط المالك يستطيع التصفير.

### ✅ التحقق من ظهور بيانات العميل للسائق
**تم التأكد من أن بوابة السائق تعرض:**
- اسم العميل
- رقم هاتف العميل (قابل للضغط للاتصال)
- عنوان التوصيل (قابل للضغط لفتح الخريطة)

---

## التحديثات السابقة (يناير 2025)

### ✅ نظام المشتريات والمخزون والتصنيع الجديد

#### التدفق الكامل:
```
المورد ← المشتريات (فاتورة+صورة) ← المخزن (مواد خام) ← التصنيع (وصفات) ← الفروع ← الزبون
```

#### الميزات المُنفذة:

**1️⃣ صفحة المشتريات (`/purchases-new`):**
- إضافة وإدارة الموردين
- إنشاء فواتير شراء مع أصناف متعددة
- رفع صورة الفاتورة (إلزامي)
- زر "إرسال للمخزن" ← ينقل المواد تلقائياً
- أرشيف فواتير لكل مورد
- عرض طلبات الشراء من المخزن

**2️⃣ صفحة المخزن والتصنيع (`/warehouse-manufacturing`):**
- **تبويب المخزن:** عرض المواد الخام + زر "إضافة للتحويل"
- **تبويب التصنيع:** 
  - عرض المواد المستلمة من المخزن
  - إنشاء وصفات (منتجات مصنعة) مع المكونات
  - زر **تصنيع** لإنتاج كميات (يخصم المواد تلقائياً)
  - حساب تكلفة الوصفة تلقائياً
- **تبويب الحركات:** واردات وصادرات المخزن
- **تبويب التحويلات:** سجل التحويلات

**3️⃣ صفحة طلبات الفروع (`/branch-orders`):**
- طلب من قسم التصنيع فقط
- دورة حياة الطلب: قيد الانتظار ← موافقة ← شحن ← تسليم
- عند الشحن: خصم من التصنيع
- عند التسليم: إضافة لمخزون الفرع
- عرض مخزون كل فرع

**4️⃣ إعدادات المخزون (في الإعدادات):**
- اختيار نوع المخزون:
  - مخزون مركزي (موزع على الفروع)
  - مخزون منفصل لكل فرع
- الخصم التلقائي عند البيع (تشغيل/إيقاف)
- تنبيهات نقص المخزون (تشغيل/إيقاف)

**5️⃣ حقول جديدة للمنتجات:**
- **تكلفة التغليف:** يُضاف للتكلفة عند البيع سفري/توصيل
- **ربط بمنتج مصنع:** للخصم التلقائي من مخزون الفرع عند البيع

**6️⃣ تكامل POS مع النظام الجديد:**
- حساب تكلفة التغليف للطلبات السفري/التوصيل
- خصم تلقائي من مخزون الفرع عند البيع
- حساب الربح الحقيقي شامل التغليف

---

## الميزات السابقة

### نظام الوصفات والمخزون (الإصدار السابق)
- المنتجات النهائية مع الوصفات
- خصم المواد الخام عند طلبات الفروع
- شجرة المواد في صفحة المخزون

### نظام صلاحيات لوحة التحكم
- صلاحيات dashboard_stats_filters
- صلاحيات dashboard_day_management
- تحكم في ظهور عناصر الواجهة

### نقاط البيع (POS)
- أنواع الطلبات: محلي، سفري، توصيل
- اختيار الطاولة للطلبات المحلية
- اختيار السائق للتوصيل
- دعم أجهزة التنبيه (buzzer)
- حساب الخصومات والضرائب

### التوصيل
- إدارة السائقين
- تتبع الطلبات
- بوابة السائق (PWA)
- خريطة السائقين

### التقارير
- تقارير المبيعات
- تقارير المصاريف
- تقارير الأرباح
- تقارير ذكية

---

## APIs الجديدة

### المشتريات
- `POST /api/suppliers` - إضافة مورد
- `GET /api/suppliers` - جلب الموردين
- `POST /api/purchases-new` - إنشاء فاتورة شراء
- `POST /api/purchases-new/{id}/upload-invoice` - رفع صورة الفاتورة
- `POST /api/purchases-new/{id}/send-to-warehouse` - إرسال للمخزن
- `GET /api/warehouse-purchase-requests` - طلبات الشراء من المخزن

### المخزون (المواد الخام)
- `POST /api/raw-materials-new` - إضافة مادة خام
- `GET /api/raw-materials-new` - جلب المواد الخام
- `POST /api/warehouse-to-manufacturing` - تحويل للتصنيع
- `GET /api/warehouse-transfers` - جلب التحويلات
- `GET /api/warehouse-transactions` - جلب الحركات

### التصنيع
- `GET /api/manufacturing-inventory` - مخزون التصنيع
- `POST /api/manufactured-products` - إضافة منتج مصنع (وصفة)
- `GET /api/manufactured-products` - جلب المنتجات المصنعة
- `POST /api/manufactured-products/{id}/produce` - تصنيع كمية

### طلبات الفروع
- `POST /api/branch-orders-new` - إنشاء طلب فرع
- `GET /api/branch-orders-new` - جلب الطلبات
- `PATCH /api/branch-orders-new/{id}/status` - تحديث الحالة
- `GET /api/branch-inventory/{branch_id}` - مخزون الفرع

### الإعدادات
- `GET /api/inventory-settings` - جلب إعدادات المخزون
- `PUT /api/inventory-settings` - تحديث إعدادات المخزون
- `GET /api/inventory-stats` - إحصائيات المخزون

---

## المهام القادمة

### أولوية عالية (P1)
1. ~~إعداد نظام المشتريات والمخزون الجديد~~ ✅
2. ~~ربط POS بالنظام الجديد~~ ✅
3. ~~إضافة كلفة التغليف~~ ✅
4. ~~إعدادات المخزون (مركزي/منفصل)~~ ✅
5. ~~تقارير المخزون والتصنيع~~ ✅
6. متابعة مشكلة بيانات العميل للسائق (التحقق)

### أولوية متوسطة (P2)
- إعادة هيكلة server.py (تقسيم الملف الضخم)
- صلاحيات القائمة الجانبية
- تحسين PWA

### أولوية منخفضة (P3)
- إعداد SendGrid لإرسال التقارير
- تحسينات نظام ولاء العملاء
- تحسينات خريطة السائقين

---

## بيانات الاختبار

### المدير
- البريد: admin@maestroegp.com
- كلمة المرور: admin123

### مدير الفرع
- البريد: manager@test.com
- كلمة المرور: 123456

---

## الهيكل التقني

```
/app
├── backend/
│   ├── routes/
│   │   └── inventory_system.py  # نظام المخزون الجديد
│   ├── server.py                # الخادم الرئيسي
│   └── requirements.txt
├── frontend/
│   └── src/
│       ├── pages/
│       │   ├── PurchasesPage.js        # صفحة المشتريات
│       │   ├── WarehouseManufacturing.js # المخزن والتصنيع
│       │   ├── BranchOrders.js         # طلبات الفروع
│       │   ├── Settings.js             # الإعدادات
│       │   ├── POS.js                  # نقاط البيع
│       │   └── DriverPortal.js         # بوابة السائق
│       └── components/
└── memory/
    └── PRD.md
```

---

## ملاحظات تقنية

- **قاعدة البيانات:** MongoDB
- **الخادم:** FastAPI (Python)
- **الواجهة:** React + Tailwind CSS + shadcn/ui
- **المصادقة:** JWT
- **الملفات:** حفظ محلي في `/app/backend/uploads/`
